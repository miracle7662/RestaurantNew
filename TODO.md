# TODO

- [ ] Modify `SettlementModal` so that Enter key flow matches requirement:
  - 1st Enter on focused payment mode selects/toggles it
  - 2nd Enter immediately triggers `Settle & Print` (handleSettle)
- [ ] Ensure flow resets after settling and when modal closes / payment mode changes
- [ ] Quick test: open settlement modal, use Enter to settle after selecting payment mode
