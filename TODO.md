# TODO: Generate React Form Component with Formik and Yup

- [ ] Create `src/common/validators.ts` with Yup schema `myFormValidationSchema` for country fields (country_name, country_code, country_capital, status).
- [ ] Create `src/components/FormikTextInput.tsx` as a reusable component for text inputs.
- [ ] Create `src/components/FormikSelect.tsx` as a reusable component for select dropdowns.
- [ ] Create `src/components/MyForm.tsx` as the main form component using Formik, forwardRef, and the custom components.
- [ ] Ensure Yup is installed via npm if not present.
- [ ] Test the component integration.
