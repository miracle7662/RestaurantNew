# TODO

- [x] Update `AtGlancePage.tsx` to show horizontal left-to-right status strip
  - [x] Compute status counts (group by status)
  - [x] Use backend `status_color` for background and auto-contrast text
  - [x] Add an “All” block
  - [x] Make status blocks clickable to set `selectedStatus` and filter the table
  - [x] Keep existing room-wise table and its filter behavior


